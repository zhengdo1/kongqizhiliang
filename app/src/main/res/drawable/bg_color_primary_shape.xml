<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" >
    <!-- 填充 -->
    <solid android:color="@android:color/transparent" /> <!-- 定义填充的颜色值 -->
    <!-- 描边 -->
    <stroke
        android:width="1dp"
        android:color="@color/datatitle" /> <!-- 定义描边的宽度和描边的颜色值 -->

    <padding
        android:bottom="0.5dp"
        android:left="0.5dp"
        android:right="1dp"
        android:top="0.5dp" />
</shape>